const person = {
  firstName: 'Mario',
  lastName: 'Rossi',
  age: 25
}

// Print values of person using Object.keys
Object.keys(person);

for( const key of Object.keys(person)){
  console.log(person );
}

